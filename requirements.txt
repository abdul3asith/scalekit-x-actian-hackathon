# Web framework
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
python-multipart>=0.0.9
itsdangerous>=2.2

# Config
pydantic-settings>=2.4
python-dotenv>=1.0

# PostgreSQL (schedule store)
psycopg[binary,pool]>=3.2

# Nebius (OpenAI-compatible client for chat + embeddings)
openai>=1.40
httpx>=0.27

# Versions the Actian client needs. We deliberately keep these ABOVE Scalekit's
# caps (protobuf<7, grpcio-status<1.67); Scalekit is installed separately with
# --no-deps so these win. protobuf is held on the 6.x line (Scalekit's generated
# *_pb2 modules don't load on 7.x). See scripts/install.sh and the README.
protobuf>=6.31.1,<7
grpcio>=1.67.0
grpcio-status>=1.67.0
